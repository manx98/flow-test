# Legacy Python Backend

This directory contains the previous Python implementation moved out of the repository root.

- `server/`: legacy FastAPI backend
- `visauto/`: legacy Python automation and vision package
- `tests/`: legacy Python tests
- `requirements.txt` / `pytest.ini`: legacy Python test/runtime configuration

The active backend is now the Go module in the repository root.
