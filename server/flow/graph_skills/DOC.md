# Graph Skills

Graph Skills 是 graph 功能模块的说明目录。目录结构负责组织能力，`node.json` 负责结构化定义，`DOC.md` 只写人工维护的使用建议。

## 使用原则

- 先根据任务目标选择能力分类，再阅读相关节点的合并文档。
- 节点类型、端口、属性、脚本函数等结构化信息不要手写在 DOC 中，由 `node.json` 自动生成。
- DOC 只描述何时使用、怎样组合、常见注意事项和经验规则。
- AI Builder 会把目录说明、分类说明、节点结构和节点用法合并后提供给模型。
