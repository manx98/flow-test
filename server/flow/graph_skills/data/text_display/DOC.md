# 文本展示

展示连入的文本变量，并原样透传文本。

## 使用建议

- 适合插在 `var/get`、HTTP 响应 body、脚本输出或 JSON/表单转换结果后面查看文本值。
- 有执行流连入时，运行到该节点会刷新画布上的展示内容。
- 未连入文本时显示 placeholder 属性；placeholder 只用于展示，不参与计算。
- 需要写入测试报告时使用日志节点；文本展示节点只做画布可视化。
