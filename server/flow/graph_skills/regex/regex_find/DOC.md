# 正则查找 (regex/find)

在文本中查找第一个匹配正则的片段。

## 使用建议

- `text` 接待搜索文本，`pattern` 接正则表达式，也可以直接写在属性里。
- 输出 `ok` 给 `flow/if` 或断言使用。正则里有捕获组时，`match` 输出第一个捕获组；没有捕获组时输出完整匹配。
- 接执行线时，匹配走 `found` 分支，不匹配走 `notFound` 分支。
